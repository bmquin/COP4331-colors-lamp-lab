#This file was created using AI, check README for proper attribution
name: Tests

on:
  push:
    branches:
      - main
      - a5-testing
  pull_request:
    branches:
      - main
      - a5-testing

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests
        run: npm test -- --testPathPattern="code.test.js"

      - name: Run integration tests
        run: npm test -- --testPathPattern="integration.test.js"

      - name: Run all tests with coverage
        run: npm test -- --coverage
